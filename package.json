{
  "name": "@specodec/typespec-emitter-ts",
  "version": "0.0.25",
  "type": "module",
  "main": "dist/index.js",
  "typespec": {
    "emitter": true,
    "options": {
      "@specodec/typespec-emitter-ts": {
        "type": "object",
        "properties": {
          "emitter-output-dir": {
            "type": "string"
          },
          "ignore-reserved-keywords": {
            "type": "boolean"
          }
        },
        "required": [
          "emitter-output-dir"
        ]
      }
    }
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@specodec/typespec-emitter-core": "^0.0.24"
  },
  "devDependencies": {
    "@typespec/compiler": "1.11.0",
    "@typespec/http": "1.11.0",
    "typescript": "^5.8.0"
  }
}